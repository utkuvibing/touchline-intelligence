# WP2.8 reproducible calibrated-model release

This packet qualifies the frozen WP2.4 full-minus-presence logistic artifact together with the frozen WP2.7 Platt decision. The reproduction scope is development-only; WC2022 and Euro2024 rows were not loaded, preprocessed, scored, or passed to the historical training process.

Comparison mode: `exact`.
Exact environment match: `true`.
Serving is intentionally deferred to M3; this packet does not add an API or UI claim.
