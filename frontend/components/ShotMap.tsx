/**
 * Compatibility export for the original M0 shot-map name.
 *
 * WP3.2 owns the model-aware map and its explicit probability/outcome/selection encodings.
 */
export { ModelShotMap as ShotMap } from "@/components/analyst/ModelShotMap";
