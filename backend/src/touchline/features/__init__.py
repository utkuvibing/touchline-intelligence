"""Derived model features.

Nothing in this package reads the target, fits anything, or touches the database. WP2.2 Slice A
contains pure shot geometry only; the categorical/context feature pipeline and the
training/serving contract belong to Slice B and are deliberately absent.
"""
